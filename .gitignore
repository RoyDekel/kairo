# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# Environment variables & secrets
.env
.env.production
.env*.local
.env.development.local
.env.test.local
.env.production.local
!.env.example

# Local checkout of the HF Hub repo backing the Chronos-2 Dedicated Inference Endpoint
# (roydekel/chronos-2-kairo) — its own git repo/remote, not part of this one. Carries
# ~478MB of model weights; never commit it here.
/chronos-2-kairo/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/
